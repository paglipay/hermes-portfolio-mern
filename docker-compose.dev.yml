version: '3.8'
services:
  backend:
    volumes:
      - ./backend:/app
    command: npm run dev
    environment:
      - MONGODB_URI=mongodb://hermes_mongo:27017/portfolio
    ports:
      - "5000:5000"
